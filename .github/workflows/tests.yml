name: CI - Tests

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  unit-tests:
    name: Tests unitaires
    runs-on: ubuntu-latest

    steps:
      - name: Checkout du code
        uses: actions/checkout@v4

      - name: Installation de .NET 8
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restauration des packages NuGet
        run: dotnet restore Billetterie-Spectacles/Billetterie-Spectacles.slnx

      - name: Compilation
        run: dotnet build Billetterie-Spectacles/Billetterie-Spectacles.slnx --no-restore

      - name: Exécution des tests unitaires
        run: dotnet test Billetterie-Spectacles/Billetterie-Spectacles.slnx --no-build --verbosity normal --filter "Category=Unit"

  integration-tests:
    name: Tests d'intégration
    runs-on: ubuntu-latest

    services:
      sqlserver:
        image: mcr.microsoft.com/mssql/server:2022-latest
        env:
          SA_PASSWORD: "TestPassword123!"
          ACCEPT_EULA: "Y"
        ports:
          - 1433:1433
        options: >-
          --health-cmd "/opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P TestPassword123! -Q 'SELECT 1' -No"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10

    steps:
      - name: Checkout du code
        uses: actions/checkout@v4

      - name: Installation de .NET 8
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restauration des packages NuGet
        run: dotnet restore Billetterie-Spectacles/Billetterie-Spectacles.slnx

      - name: Compilation
        run: dotnet build Billetterie-Spectacles/Billetterie-Spectacles.slnx --no-restore

      - name: Exécution des tests d'intégration
        env:
          ConnectionStrings__TestDatabase: "Server=localhost,1433;Database=BilletterieTests;User ID=sa;Password=TestPassword123!;TrustServerCertificate=True;"
        run: dotnet test Billetterie-Spectacles/Billetterie-Spectacles.slnx --no-build --verbosity normal --filter "Category=Integration"